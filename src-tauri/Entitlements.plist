<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- An item guarded by Touch ID lives in the data protection keychain,
         and macOS only lets an app in there if it is signed with a keychain
         access group. Without this the Keychain answers every write with
         errSecMissingEntitlement. The group is the bundle identifier: a
         Developer ID build would prefix it with the team identifier. -->
    <key>keychain-access-groups</key>
    <array>
        <string>dgrr.tray.byrgi</string>
    </array>
</dict>
</plist>
