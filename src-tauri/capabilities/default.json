{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The signer window talks only to this app's own commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
